## v1.2.1 — 修复：预览模式异常显示编辑器、空行被折叠、切换滚动位置丢失

本次更新修复了三个问题：

### 🐛 修复

- **预览模式不再显示编辑器**：修复了预览模式下仍能看到/操作编辑器的严重问题（`v-show` 对多根组件静默失效）
- **空行在预览中可见**：编辑模式多次回车产生的空白行，阅读模式现在会按同样的行数显示，不再被折叠
- **切换模式不回到顶部**：编辑 ↔ 预览切换时保留滚动位置，不再跳回便签开头

### ✨ 顺带包含 v1.2.0 的「自动更新检查」

应用启动时自动检查 GitHub Releases 是否有新版本，发现新版后后台下载，下载完成弹窗提示一键重启安装——以后不用再手动去 Releases 页下载了。

### 📦 安装

下载 `MarkdownNote-Setup-1.2.1.exe` 安装即可。本次更新功能需要**手动安装此版本启用**，之后的新版本即可自动更新。

历史版本：v1.2.0 新增自动更新检查；v1.1.0 新增文本高亮标记；v1.0.0 为初始版本。
