## v1.2.0 — 新增「自动更新检查」

应用启动时会自动检查 GitHub Releases 是否有新版本，发现新版后后台下载，下载完成弹窗提示一键重启安装——以后不用再手动去 Releases 页下载了。

### ✨ 新增功能

- **自动更新检查**：启动时静默检查新版本，失败（无网络等）不影响使用
- **下载进度提示**：发现新版自动后台下载，弹窗实时显示进度，可随时收起继续使用
- **一键安装**：下载完成后提示「重启并安装」，确认后自动完成升级，便签数据完整保留
- **稍后再说**：点「稍后」可先继续工作，退出应用时更新会自动安装

### 📦 安装

下载 `MarkdownNote-Setup-1.2.0.exe` 安装即可。本次更新功能需要**手动安装此版本启用**，之后的新版本即可自动更新。

历史版本：v1.1.0 新增文本高亮标记；v1.0.0 为初始版本。
